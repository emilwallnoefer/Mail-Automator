// Thin re-export so existing imports keep working after the settings module split.
export { SettingsPanel } from "./settings/settings-panel";
export type { SettingsSectionId } from "./settings/types";
