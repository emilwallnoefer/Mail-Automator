// Thin re-export shim: the panel was split into ./mail-tracking/. Kept so the
// existing importer (admin-panel.tsx) keeps compiling unchanged.
export { MailTrackingPanel } from "./mail-tracking/mail-tracking-panel";
